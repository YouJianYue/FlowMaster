# ========================
# FlowMaster 环境配置模板
# ========================
# 复制此文件为 .env 并根据实际环境修改配置值

# ========================
# 环境配置
# ========================
ENVIRONMENT=development
DEBUG=true
SECRET_KEY=your-super-secret-key-change-this-in-production

# ========================
# 数据库配置
# ========================
# 开发环境推荐使用 SQLite
DATABASE_URL=sqlite+aiosqlite:///./data/flowmaster.db
DATABASE_ECHO=true
DATABASE_POOL_SIZE=5
DATABASE_MAX_OVERFLOW=10
DATABASE_POOL_TIMEOUT=30

# 生产环境推荐使用 PostgreSQL
# DATABASE_URL=postgresql+asyncpg://username:password@localhost:5432/flowmaster
# DATABASE_ECHO=false
# DATABASE_POOL_SIZE=20
# DATABASE_MAX_OVERFLOW=30
# DATABASE_POOL_TIMEOUT=30

# 或使用 MySQL
# DATABASE_URL=mysql+asyncmy://username:password@localhost:3306/flowmaster
# DATABASE_ECHO=false
# DATABASE_POOL_SIZE=20
# DATABASE_MAX_OVERFLOW=30
# DATABASE_POOL_TIMEOUT=30

# ========================
# Redis配置 (可选，用于缓存)
# ========================
REDIS_URL=redis://127.0.0.1:6379/0
# REDIS_PASSWORD=your-redis-password-if-needed

# ========================
# JWT认证配置
# ========================
JWT_SECRET_KEY=change-this-super-secret-jwt-key-in-production
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7
JWT_ALGORITHM=HS256

# ========================
# RSA加密配置
# ========================
# 在线生成 RSA 密钥对: http://web.chacuo.net/netrsakeypair
FLOWMASTER_SECURITY_CRYPTO_PRIVATE_KEY=your-rsa-private-key-here
FLOWMASTER_SECURITY_CRYPTO_PUBLIC_KEY=your-rsa-public-key-here

# ========================
# 验证码配置
# ========================
CAPTCHA_MAIL_LENGTH=6
CAPTCHA_MAIL_EXPIRATION_IN_MINUTES=5
CAPTCHA_SMS_LENGTH=6
CAPTCHA_SMS_EXPIRATION_IN_MINUTES=5

# ========================
# 日志配置
# ========================
LOG_LEVEL=DEBUG
LOG_FILE_PATH=./logs
LOG_MAX_SIZE=20971520
LOG_BACKUP_COUNT=5

# ========================
# 应用服务配置
# ========================
APP_HOST=0.0.0.0
APP_PORT=8000
APP_RELOAD=true

# ========================
# 邮件配置 (可选)
# ========================
# MAIL_SERVER=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USERNAME=your-email@gmail.com
# MAIL_PASSWORD=your-app-password
# MAIL_USE_TLS=true

# ========================
# 第三方登录配置 (可选)
# ========================
# GitHub OAuth
# GITHUB_CLIENT_ID=your-github-client-id
# GITHUB_CLIENT_SECRET=your-github-client-secret

# Gitee OAuth  
# GITEE_CLIENT_ID=your-gitee-client-id
# GITEE_CLIENT_SECRET=your-gitee-client-secret

# ========================
# 文件存储配置 (可选)
# ========================
# 本地存储根路径
STORAGE_LOCAL_ROOT_PATH=./data/files

# 自定义访问域名（可选，留空则使用相对路径）
# STORAGE_LOCAL_DOMAIN=https://cdn.example.com

# 最大文件大小（字节），默认100MB
STORAGE_MAX_FILE_SIZE=104857600

# 文件名最大长度，默认255
STORAGE_MAX_FILENAME_LENGTH=255

# 允许上传的文件扩展名（逗号分隔）
STORAGE_ALLOWED_EXTENSIONS=pdf,doc,docx,xls,xlsx,ppt,pptx,txt,md,rtf,jpg,jpeg,png,gif,bmp,svg,webp,ico,mp3,wav,wma,aac,flac,ogg,m4a,mp4,avi,mov,wmv,flv,mkv,webm,mpg,mpeg,zip,rar,7z,tar,gz,bz2,xz